# Canonical site URL used by every metadata/SEO file (see lib/site.ts).
# Falls back to the Vercel deployment URL if unset. Set this to
# https://vyshakharikumar.dev once the custom domain is live, in
# Vercel's project settings (or a local .env.local for testing) —
# no code changes needed.
NEXT_PUBLIC_SITE_URL=https://vyshak-harikumar.vercel.app
