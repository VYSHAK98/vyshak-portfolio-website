# Canonical site URL used by every metadata/SEO file (see lib/site.ts).
# The Vercel URL is the permanent site URL — no custom domain is
# planned. Only change this (in Vercel's project settings, or a local
# .env.local for testing) if that ever changes — no code changes needed.
NEXT_PUBLIC_SITE_URL=https://vyshak-harikumar.vercel.app
